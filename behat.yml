default:
  paths:
    bootstrap:  '%behat.paths.features%/bootstrap'
  extensions:
    Behat\MinkExtension\Extension:
      base_url: URI_UNDER_TEST
      javascript_session: sahi
      browser_name: phantomjs
      goutte: ~
      sahi: ~
